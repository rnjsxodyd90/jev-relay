import http from 'node:http';import{readFile}from'node:fs/promises';import{fileURLToPath}from'node:url';
const allowed=new Set(['dashboard.html','results.json','cases.json','summary.json','README.md']);
http.createServer(async(req,res)=>{if(req.headers.host!=='127.0.0.1:4430'||req.method!=='GET'){res.writeHead(403);res.end();return;}const filename=req.url==='/'?'dashboard.html':req.url.slice(1);if(!allowed.has(filename)){res.writeHead(404);res.end();return;}try{const data=await readFile(fileURLToPath(new URL(filename,import.meta.url)));res.writeHead(200,{'Content-Type':filename.endsWith('.html')?'text/html; charset=utf-8':filename.endsWith('.json')?'application/json; charset=utf-8':'text/plain; charset=utf-8','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});res.end(data);}catch{res.writeHead(500);res.end();}}).listen(4430,'127.0.0.1',()=>console.log('Recorded benchmark: http://127.0.0.1:4430'));
